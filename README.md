# Magic Button: Word ↔ Excel

> Быстрый и простой конвертер таблиц между Word и Excel для Windows

![Version](https://img.shields.io/badge/version-2.0-blue)
![Python](https://img.shields.io/badge/python-3.7+-brightgreen)
![License](https://img.shields.io/badge/license-MIT-green)

## 🎯 Что это?

**Magic Button** - это Windows-приложение, которое помогает переносить таблицы между документами Word (.docx) и электронными таблицами Excel (.xlsx) одним нажатием кнопки.

Идеально для:
- 📋 Быстрого преобразования отчетов и данных
- 🔄 Передачи таблиц между системами
- ⚡ Массовой обработки нескольких файлов
- 💼 Подготовки документов для отправки

## ✨ Возможности

### Mode 1: Word → Excel 📄
- Берет **ВСЕ** файлы `.docx` из выбранной папки
- Извлекает все таблицы из каждого файла
- Каждую таблицу сохраняет на **отдельный лист** в Excel
- Автоматически очищает пустые строки
- Убирает лишние пробелы в ячейках
- Обрабатывает поврежденные файлы без сбоев

### Mode 2: Excel → Word 📊
- Берет выбранный `.xlsx` файл
- Все непустые листы преобразует в Word-таблицы
- Каждый лист становится отдельной таблицей с заголовком
- Автоматически удаляет пустые столбцы
- Аккуратно форматирует текст

## 🚀 Быстрый старт

### Требования
- Windows 7/8/10/11
- Python 3.7+ (с галочкой "Add Python to PATH" при установке)

### Установка

1. **Скачайте проект:**
   ```bash
   git clone https://github.com/MakarSaltykov-glitch/Magic_Button.git
   cd Magic_Button
   ```

2. **Установите зависимости** (первый раз):
   - Дважды нажмите файл `install_requirements.bat`
   - Дождитесь окончания установки
   - Закройте окно

3. **Запустите программу:**
   - Дважды нажмите файл `run_docx_tables_app.bat`
   - Выберите нужный режим
   - Укажите файлы и нажмите "Начать"

## 📖 Как использовать

### Word → Excel

```
1. Откройте программу (двойной клик по run_docx_tables_app.bat)
2. Перейдите на вкладку "📄 Word → Excel"
3. Нажмите "Выбрать" и укажите папку с файлами .docx
4. Нажмите "Сохранить как" и выберите имя для файла .xlsx
5. Нажмите "Начать: Word → Excel"
6. Смотрите ход работы в журнале
7. Дождитесь сообщения "Готово"
```

**Результат:** В выходном Excel-файле будет столько листов, сколько всего таблиц найдено во всех Word-файлах.

### Excel → Word

```
1. Откройте программу (двойной клик по run_docx_tables_app.bat)
2. Перейдите на вкладку "📊 Excel → Word"
3. Нажмите "Выбрать" и укажите .xlsx файл
4. Нажмите "Сохранить как" и выберите имя для файла .docx
5. Нажмите "Начать: Excel → Word"
6. Смотрите ход работы в журнале
7. Дождитесь сообщения "Готово"
```

**Результат:** Word-файл с таблицами из всех листов Excel.

## 🔧 Сборка EXE

Чтобы создать standalone .exe файл:

1. Убедитесь, что установлены все зависимости:
   ```bash
   pip install -r requirements.txt pyinstaller
   ```

2. Запустите скрипт сборки:
   - На Windows: дважды нажмите `build_exe.bat`
   - Или в терминале: `python -m PyInstaller --noconfirm --onefile --windowed --name MagicButton docx_tables_to_excel_app.py`

3. Готовый exe файл будет в папке `dist\MagicButton.exe`

## 📦 Структура проекта

```
Magic_Button/
├── docx_tables_to_excel_app.py      # Основная программа
├── requirements.txt                  # Зависимости Python
├── install_requirements.bat          # Установка зависимостей
├── run_docx_tables_app.bat          # Запуск программы
├── build_exe.bat                     # Сборка exe
└── README.md                         # Этот файл
```

## 🔗 Зависимости

Программа использует только стандартные библиотеки:
- **python-docx** - работа с Word документами
- **pandas** - работа с таблицами данных
- **openpyxl** - работа с Excel файлами
- **tkinter** - графический интерфейс (встроен в Python)

## ⚠️ Важно знать

- ✅ Поддерживаются только `.docx` и `.xlsx` (новые форматы)
- ❌ Старые форматы `.doc` и `.xls` **не поддерживаются**
- ✅ Работает с таблицами любого размера
- ✅ Автоматически пропускает пустые таблицы и листы
- ✅ Обрабатывает поврежденные файлы без краша
- 📝 Весь процесс логируется в окне программы

## 🐛 Если что-то не работает

**Проблема:** "tkinter not found"
```
Решение: При установке Python поставьте галочку "tcl/tk and IDLE"
```

**Проблема:** "No module named 'docx'"
```
Решение: Запустите install_requirements.bat ещё раз
```

**Проблема:** "Access denied" при сохранении
```
Решение: Закройте файл в Excel/Word перед конвертацией
```

## 📄 Лицензия

MIT License - смотрите файл LICENSE для деталей

## 👨‍💻 Автор

[MakarSaltykov-glitch](https://github.com/MakarSaltykov-glitch)

## 💡 Идеи и улучшения

Если у вас есть идеи для улучшения - создавайте Issues в репозитории!

---

**Нравится проект?** ⭐ Поставьте звездочку в GitHub!
2. В поле `Папка с Word-файлами .docx` выберите папку с документами Word.
3. В поле `Куда сохранить Excel .xlsx` выберите имя будущего Excel-файла.
4. Нажмите `Начать: Word в Excel`.

Программа просмотрит все `.docx` файлы в папке и сохранит все найденные таблицы в один Excel-файл.

## Как перенести Excel в Word

1. Откройте вкладку `Excel в Word`.
2. В поле `Excel-файл .xlsx` выберите Excel-файл.
3. В поле `Куда сохранить Word .docx` выберите имя будущего Word-файла.
4. Нажмите `Начать: Excel в Word`.

Программа возьмет все непустые листы Excel и создаст Word-документ с таблицами.

## Как сделать обычный `.exe` файл

Это необязательно. Программа уже запускается через `run_docx_tables_app.bat`.

Если нужен один файл приложения:

1. Дважды нажмите `build_exe.bat`.
2. Дождитесь окончания сборки.
3. Готовый файл появится в папке `dist`.
4. Запускайте файл `MagicButton.exe`.

Сборку `.exe` лучше делать на Windows.

## Если что-то пошло не так

- Если программа не запускается, проверьте, установлен ли Python.
- Если окно быстро закрывается, сначала запустите `install_requirements.bat`.
- Если итоговый файл уже открыт в Word или Excel, закройте его и повторите запуск.
- Если файл поврежден, программа пропустит его и напишет сообщение в журнал.
- Если нужен старый формат `.doc` или `.xls`, сначала откройте файл в Word или Excel и сохраните его как `.docx` или `.xlsx`.

## Важные заметки

- Программа работает только с `.docx` и `.xlsx`.
- Старые файлы Word `.doc` и Excel `.xls` не поддерживаются.
- Если итоговый Word или Excel-файл уже открыт, закройте его перед запуском программы.
- Если в папке нет `.docx`, программа покажет ошибку.
- Если в документе нет таблиц, программа напишет об этом в журнале.

## Какие файлы здесь есть

- `docx_tables_to_excel_app.py` - сама программа с интерфейсом.
- `run_docx_tables_app.bat` - запуск программы на Windows.
- `install_requirements.bat` - установка нужных библиотек.
- `build_exe.bat` - сборка обычного `.exe` файла на Windows.
- `requirements.txt` - список библиотек Python.
